<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/side_nav_home_group">

        <item android:id="@+id/side_nav_home"
            android:title="@string/home"
            android:icon="@drawable/home_icon"
            />
        <item android:id="@+id/history"
            android:title="History"
            android:icon="@drawable/history_icon"
            />
        <item android:id="@+id/settings"
            android:title="Settings"
            android:icon="@drawable/settings_icon"
            />
    </group>

    <group android:id="@+id/side_nav_logout_group">

        <item android:id="@+id/side_nav_logout"
            android:title="@string/logout"
            android:icon="@drawable/logout_icon"
            />
        <!--<item android:id="@+id/side_nav_home"
            android:title="Home"
            android:icon="@drawable/home_icon"
            />
        <item android:id="@+id/side_nav_home"
            android:title="Home"
            android:icon="@drawable/home_icon"
            />
        <item android:id="@+id/side_nav_home"
            android:title="Home"
            android:icon="@drawable/home_icon"
            />-->
    </group>


</menu>